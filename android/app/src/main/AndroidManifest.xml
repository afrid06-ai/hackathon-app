<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CloudBudget"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CloudBudget"
        android:usesCleartextTraffic="true">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CloudBudget.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:theme="@style/Theme.CloudBudget" />

        <!-- Main App -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.CloudBudget" />

        <activity android:name=".ui.auth.LoginActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.auth.ForgotPasswordActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.auth.CloudCredentialsActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.settings.SettingsActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.settings.ProfileActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.settings.ManageCloudsActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.settings.NotificationPreferencesActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.settings.SecuritySettingsActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.detail.AlertDetailActivity" android:theme="@style/Theme.CloudBudget" />
        <activity android:name=".ui.detail.CloudDetailActivity" android:theme="@style/Theme.CloudBudget" />

    </application>
</manifest>
